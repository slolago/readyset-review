---
phase: 46-comments-integrity-and-range
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/viewer/CommentSidebar.tsx
  - src/components/viewer/CommentItem.tsx
  - src/components/viewer/VideoPlayer.tsx
  - src/app/(app)/projects/[projectId]/assets/[assetId]/page.tsx
  - src/app/review/[token]/page.tsx
  - src/app/api/assets/route.ts
  - src/app/api/assets/[assetId]/route.ts
autonomous: false
requirements:
  - CMT-01
  - CMT-02
  - CMT-03
must_haves:
  truths:
    - "User can mark in-point + out-point on a new comment; the timeline renders a highlighted range for that comment"
    - "Clicking a range-comment marker on the timeline seeks to its in-point"
    - "The range-marker tooltip shows both the in-point and out-point in mm:ss"
    - "CommentItem shows a range badge (in -> out) when a comment has inPoint + outPoint, and clicking the body seeks to inPoint"
    - "Grid card comment-count badge equals the number of non-resolved, user-visible comments (drawings without text cannot exist because Send is disabled; count does not include comments with empty/whitespace text)"
    - "Sidebar tab count matches grid badge count for the same asset"
    - "Switching to a different asset/version clears any pending annotation so a drawing never persists without text"
    - "Send button is disabled when comment text is empty, even if a drawing or range is attached"
  artifacts:
    - path: src/components/viewer/CommentItem.tsx
      provides: "Range badge display and in-point seek when clicking a range comment"
      contains: "inPoint"
    - path: src/components/viewer/VideoPlayer.tsx
      provides: "Range-marker tooltip showing in/out endpoints"
      contains: "outPoint"
    - path: src/app/api/assets/route.ts
      provides: "_commentCount derivation that counts only user-visible comments (non-empty text)"
      contains: "_commentCount"
    - path: src/app/(app)/projects/[projectId]/assets/[assetId]/page.tsx
      provides: "Clears pendingAnnotation when the displayed asset changes"
      contains: "pendingAnnotation"
  key_links:
    - from: src/components/viewer/CommentSidebar.tsx
      to: src/app/api/comments/route.ts
      via: "onAddComment with inPoint/outPoint/timestamp"
      pattern: "inPoint"
    - from: src/components/viewer/VideoPlayer.tsx
      to: src/components/viewer/CommentSidebar.tsx
      via: "onCommentClick for range markers seeks to inPoint"
      pattern: "rangeComments"
    - from: src/app/api/assets/route.ts
      to: "Firestore comments collection"
      via: "filter c.text.trim() before counting"
      pattern: "_commentCount"
---

<objective>
Phase 46: comments-integrity-and-range — finish the in/out range comment flow (CMT-01), make the comment-count badge reflect what the user actually sees (CMT-02), and guarantee no orphan drawings are ever persisted (CMT-03).

Purpose: Comments must be trustworthy. The range composer UI exists but lacks polish (no range badge in comment list, no tooltip on range markers). Count derivation double-counts edge cases. Annotation drawings can linger in component state across asset switches.

Output: A polished range-comment experience, a consistent comment count across grid and sidebar, and airtight composer state that cannot leak orphan drawings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-comments-integrity-and-range/46-CONTEXT.md

@src/types/index.ts
@src/components/viewer/CommentSidebar.tsx
@src/components/viewer/CommentItem.tsx
@src/components/viewer/VideoPlayer.tsx
@src/app/(app)/projects/[projectId]/assets/[assetId]/page.tsx
@src/app/api/comments/route.ts
@src/app/api/assets/route.ts

<interfaces>
<!-- Already-existing contracts the executor should use directly -->

From src/types/index.ts:
```typescript
export interface Comment {
  id: string;
  assetId: string;
  projectId: string;
  reviewLinkId?: string;
  authorId: string | null;
  authorName: string;
  authorEmail?: string;
  text: string;
  timestamp?: number; // video seconds
  inPoint?: number;   // range start in video seconds
  outPoint?: number;  // range end in video seconds
  annotation?: AnnotationData;
  resolved: boolean;
  parentId: string | null;
  createdAt: Timestamp;
}

export interface Asset {
  // ...
  _commentCount?: number;
}
```

From src/components/viewer/CommentSidebar.tsx:
- Composer already has IN / OUT buttons wired to local `inPoint` / `outPoint` state
  and passes them into `onAddComment` when both are set (lines 110-116, 288-324).
- `handleSubmit` already guards on `!text.trim()` — Send button already disabled (line 338).

From src/components/viewer/VideoPlayer.tsx:
- `rangeComments` is already derived (line 355-357) and renders as a highlighted
  bar on the timeline (lines 485-498). Currently uses only a `title=` attribute —
  no styled tooltip showing both endpoints.
- `timedComments` markers already have a rich tooltip (lines 517-549). Model the
  range-marker tooltip on that existing pattern.

From src/app/api/comments/route.ts (POST):
- Already accepts `inPoint`, `outPoint`, `timestamp` and writes them through.
  No API changes needed for range comments.

From src/app/api/assets/route.ts (GET, lines 51-63):
- `_commentCount` is built by a single Firestore `where('projectId','==',projectId)`
  query and increments per assetId. It currently counts EVERY comment doc —
  including resolved comments and (theoretically) docs with empty `text`.

From src/app/(app)/projects/[projectId]/assets/[assetId]/page.tsx:
- Holds `pendingAnnotation` in state. `setPendingAnnotation(null)` is already
  called in several flows, but NOT when `displayAsset.id` changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Range tooltip on VideoPlayer timeline markers</name>
  <files>src/components/viewer/VideoPlayer.tsx</files>
  <action>
    In the range-comment marker rendering (current lines 485-498), wrap the button in a hover-aware container that shows a styled tooltip (mirror the `timedComments` tooltip pattern at lines 517-549) instead of the plain `title=` attribute.

    Tooltip contents:
    - Author name
    - "IN {mm:ss} - OUT {mm:ss}" using `formatDuration(c.inPoint ?? 0)` and `formatDuration(c.outPoint ?? 0)`
    - First 120 chars of `c.text` (line-clamp-3)

    Reuse the existing edge-shift pattern (pct &lt; 20 / pct &gt; 80 / centered) so the tooltip doesn't clip off the timeline edges; use `startPct` as the anchor.

    Keep the click-to-seek behavior via `onCommentClick?.(c)` — the parent already seeks to `inPoint` because the range comment carries `timestamp = inPoint` (set in CommentSidebar.handleSubmit). DO NOT change that wiring.

    Addresses CMT-01 success criterion: "clicking a range comment seeks to the in-point" + the polish sub-goal from the plan brief ("range marker tooltip shows both endpoints").
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "VideoPlayer" || echo "OK"</automated>
  </verify>
  <done>Hovering a range marker on the timeline shows a styled tooltip with author, "IN mm:ss - OUT mm:ss", and comment text preview. Clicking the marker still seeks the player to the in-point.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CommentItem range badge + in-point seek</name>
  <files>src/components/viewer/CommentItem.tsx</files>
  <behavior>
    - When `comment.inPoint !== undefined &amp;&amp; comment.outPoint !== undefined`, the timecode pill shows a two-part "mm:ss - mm:ss" range chip instead of the single timestamp chip.
    - Clicking the comment body seeks to `inPoint` (not `timestamp`, though they're equal today for range comments — prefer inPoint for semantic clarity).
    - Single-timestamp comments (no outPoint) render unchanged.
  </behavior>
  <action>
    Update `CommentItem.tsx`:

    1. Derive `const hasRange = comment.inPoint !== undefined &amp;&amp; comment.outPoint !== undefined;` near the other `has*` derivations (around line 73).

    2. In the timecode pill (lines 99-109), branch on `hasRange`:
       - If `hasRange`: render a wider pill: `<span>{formatDuration(comment.inPoint!)} - {formatDuration(comment.outPoint!)}</span>` — same color theming as the existing pill.
       - Else: keep existing single-timestamp pill.

    3. In `handleClick` (lines 76-86), change `onSeek?.(comment.timestamp!)` to `onSeek?.(hasRange ? comment.inPoint! : comment.timestamp!)`.

    DO NOT touch reply, resolve, edit, delete, or annotation UI. Match the existing pill styling (font-mono, bg-frame-accent/15 border, resolved-variant).
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "CommentItem" || echo "OK"</automated>
  </verify>
  <done>A range comment in the sidebar displays "0:03 - 0:08" in its timecode pill; clicking it seeks the video to 0:03. A non-range comment is visually unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Composer state hygiene — clear pending annotation + range on asset switch</name>
  <files>
    src/app/(app)/projects/[projectId]/assets/[assetId]/page.tsx,
    src/app/review/[token]/page.tsx,
    src/components/viewer/CommentSidebar.tsx
  </files>
  <action>
    Three changes to close the orphan-drawing window (CMT-03) and the orphan-range window by symmetry.

    (A) In `src/app/(app)/projects/[projectId]/assets/[assetId]/page.tsx` — add a useEffect that runs when `displayAsset?.id` changes and clears all composer state:
    ```tsx
    useEffect(() => {
      setIsAnnotationMode(false);
      setPendingAnnotation(null);
      setActiveAnnotationCommentId(null);
      setDisplayShapes(null);
      setSelectedCommentId(null);
    }, [displayAsset?.id]);
    ```
    Place it after the existing `setActiveVersion` effect (line 49-51). This prevents a drawing captured for Asset A from sticking around when the user switches to Asset B or to a different version.

    (B) In `src/app/review/[token]/page.tsx` — apply the same pattern for the review-link viewer. Find the equivalent `pendingAnnotation` state and add an identical cleanup effect keyed on the displayed asset id.

    (C) In `src/components/viewer/CommentSidebar.tsx` — add a cleanup effect that clears local composer state (`text`, `inPoint`, `outPoint`, `replyTo`) when `asset.id` changes:
    ```tsx
    useEffect(() => {
      setText('');
      setInPoint(undefined);
      setOutPoint(undefined);
      setReplyTo(null);
    }, [asset.id]);
    ```
    Place near the existing effects (around line 71). DO NOT clear `includeTimestamp` (user preference persists) and DO NOT reset `activeTab`.

    Also: tighten the Send-disabled guard to reject whitespace-only text explicitly. Current code already uses `!text.trim()` — verify, no change needed.

    NOTE: Send button disabled state (line 338) is already correct — leave it alone. Cancel on annotation toolbar already clears the canvas (VideoPlayer line 336-338). The gap being closed is purely the "switch asset while composing" path.

    Addresses CMT-03: no orphan drawings are written; no stale drawing leaks across assets.
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "assets/\[assetId\]|review/\[token\]|CommentSidebar" || echo "OK"</automated>
  </verify>
  <done>Opening Asset A, starting a drawing, attaching it (pending), then navigating to Asset B — the new asset's composer is empty; no drawing, no range, no text, no reply target.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Comment-count integrity — server-side derivation</name>
  <files>src/app/api/assets/route.ts, src/app/api/assets/[assetId]/route.ts</files>
  <action>
    The `_commentCount` derivation in `src/app/api/assets/route.ts` (lines 51-63) currently counts every comment doc. Make the rule explicit and consistent so the grid badge matches what the user sees in the sidebar.

    Rule (per CMT-02 + phase context): **count all top-level, non-empty comments for the asset — do not count replies, do not count comments with empty/whitespace text.** Resolved comments DO count (they remain in the sidebar with a "Completed" badge — per CommentSidebar.tsx line 84-87; the sidebar tab shows `comments.length` which includes resolved today, so grid must match).

    In `src/app/api/assets/route.ts` around lines 51-63, change the aggregation:
    ```ts
    const commentsSnap = await db.collection('comments').where('projectId', '==', projectId).get();
    for (const doc of commentsSnap.docs) {
      const d = doc.data() as any;
      const aid = d.assetId as string | undefined;
      if (!aid) continue;
      if (d.parentId) continue;                           // skip replies
      if (!d.text || !String(d.text).trim()) continue;    // skip empty/whitespace
      commentCountMap.set(aid, (commentCountMap.get(aid) ?? 0) + 1);
    }
    ```

    If `src/app/api/assets/[assetId]/route.ts` exists and also computes `_commentCount` for a single asset, apply the same rule there. If it does NOT compute `_commentCount`, leave it alone — do not add new derivation there.

    Also: update the sidebar tab count in `CommentSidebar.tsx` header to use `topLevel.length` instead of `comments.length` (line 172-176) so the tab number equals the grid badge (both exclude replies). This is a one-line change; include it here because it's the other half of the consistency rule.

    Wait — re-reading the file: `topLevel` is computed at line 87-97 and already excludes `parentId`-bearing comments. Change line 172 from `comments.length` to `topLevel.length` and line 175 from `{comments.length}` to `{topLevel.length}`.

    CommentSidebar.tsx IS already in files_modified for Task 3; the one-line header change belongs there too — do it in Task 3 alongside the cleanup effects, not here. This task is server-only.

    Revised scope for this task: server-side derivation only. Leave CommentSidebar tab count to Task 3.
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "api/assets" || echo "OK"</automated>
  </verify>
  <done>A fresh GET /api/assets?projectId=... returns `_commentCount` values that equal the number of top-level, non-empty comments per asset. Replies do not inflate the count. A manual test: asset with 2 top-level + 3 replies shows badge = 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Sidebar tab count matches grid badge</name>
  <files>src/components/viewer/CommentSidebar.tsx</files>
  <action>
    Small follow-on to Task 4 — keep the sidebar header count consistent with the grid badge (both now = top-level, non-empty).

    In `src/components/viewer/CommentSidebar.tsx`:
    - Line 172 (visibility guard): change `comments.length > 0` to `topLevel.length > 0`.
    - Line 175 (displayed number): change `{comments.length}` to `{topLevel.length}`.

    `topLevel` is already filtered to `!c.parentId` (line 91). It does not filter empty text, but text is required at POST time — so no empty-text docs will exist in practice; no additional filter needed client-side.

    DO NOT touch composer, list rendering, or any other part of this file in this task — it's 2 lines.
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "CommentSidebar" || echo "OK"</automated>
  </verify>
  <done>An asset with 2 top-level comments + 3 replies shows "Comments 2" in the tab header (not 5). Matches the grid badge for the same asset.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Range-composer UX polish — clearer empty-state + OUT-before-IN guard</name>
  <files>src/components/viewer/CommentSidebar.tsx</files>
  <action>
    Tighten the existing range-mark UI (already in composer at lines 288-324):

    1. OUT &lt; IN guard: when the user clicks OUT, if `currentTime &lt;= inPoint`, show a toast (`import toast from 'react-hot-toast'` is already imported) with message "Out-point must come after in-point" and do NOT set outPoint. Same rule if the user changes the video position backwards and clicks OUT at an earlier time.

    2. Visual hint while IN is set but OUT is not: the OUT button's label should be slightly more prominent — add `animate-pulse` (tailwind) to draw the eye, since currently it's easy to miss that you need to hit OUT.

    3. When a range is set (both IN and OUT), disable the single-timestamp toggle (`includeTimestamp`) and gray it out — range comments don't need a separate single timestamp. The existing `handleSubmit` already ignores `includeTimestamp` when both IN and OUT are set (line 110-116), so this is a UX-only tweak: `disabled={inPoint !== undefined &amp;&amp; outPoint !== undefined}` on the timestamp button (line 279-286).

    DO NOT add any new button types or keyboard shortcuts. DO NOT change the submit payload shape.
  </action>
  <verify>
    <automated>cd C:/Users/Lola/Documents/frame &amp;&amp; npx tsc --noEmit 2&gt;&amp;1 | grep -E "CommentSidebar" || echo "OK"</automated>
  </verify>
  <done>Clicking OUT at a time before IN shows a toast and does nothing. OUT button pulses while awaiting. Timestamp toggle is disabled once a full range is set.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Human verification — end-to-end range, count, orphan</name>
  <what-built>
    CMT-01 range comments polished (tooltip, CommentItem badge), CMT-02 count integrity fixed (server + sidebar tab), CMT-03 composer state isolated across asset switches + OUT-before-IN guard.
  </what-built>
  <how-to-verify>
    Run: `npm run dev`

    Scenario A — Range comment (CMT-01):
    1. Open a project with a video asset, enter the viewer.
    2. Seek to ~3s, click IN. Seek to ~8s, click OUT. Type "test range" and press Enter.
    3. Verify: a highlighted range bar appears on the timeline between 3s and 8s.
    4. Hover the range bar: tooltip shows "IN 0:03 - OUT 0:08", author, and "test range".
    5. Seek to ~15s (well past the range). Click the range bar in the timeline.
    6. Verify: video seeks to 0:03 (the in-point), not 0:15.
    7. In the sidebar, the comment shows "0:03 - 0:08" in its pill.

    Scenario B — Count integrity (CMT-02):
    1. On the same asset, reply to the range comment twice.
    2. Go back to the grid (close viewer).
    3. Verify: the asset's comment badge shows 1, not 3 (replies excluded).
    4. Reopen the viewer: tab header shows "Comments 1".

    Scenario C — Orphan drawing (CMT-03):
    1. Open Asset A, click Annotate, draw a rectangle, click "Attach to comment".
    2. Do NOT type text. Navigate to Asset B via the sidebar/grid.
    3. Verify: Asset B's composer has no attached drawing, no text, no range.
    4. Also verify: Firestore `comments` has no new doc with empty `text` (check the console or Firestore directly).

    Scenario D — OUT-before-IN guard:
    1. Seek to 10s, click IN. Seek to 5s, click OUT.
    2. Verify: toast "Out-point must come after in-point" appears. OUT is not set.

    Report pass/fail per scenario. If any fails, describe what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues per scenario</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits clean.
- No new lint warnings in `CommentSidebar`, `CommentItem`, `VideoPlayer`, viewer pages, or `api/assets/route.ts`.
- Grid badge count = sidebar tab count for the same asset across 3+ test assets.
- No persisted comment docs with `text === ''` or `text.trim() === ''` after the CMT-03 flows.
</verification>

<success_criteria>
1. User can set IN + OUT on a new comment; timeline shows highlighted range with a rich tooltip (IN mm:ss - OUT mm:ss); clicking the range seeks to the in-point. (CMT-01 ✓)
2. Grid badge and sidebar tab count both reflect top-level, non-empty comments only — replies excluded, empties excluded. (CMT-02 ✓)
3. Switching assets clears composer state; Send is disabled on empty text; no orphan drawings reach Firestore. OUT-before-IN is rejected. (CMT-03 ✓)
</success_criteria>

<output>
After completion, create `.planning/phases/46-comments-integrity-and-range/46-01-SUMMARY.md`
</output>
