---
phase: 66
plan: 01
type: atomic
autonomous: true
requirements: [CLN-01, CLN-02, CLN-03, CLN-04, CLN-05, CLN-06, CLN-07]
---

# Phase 66 Plan 01: Dead Data & Contracts Cleanup

Seven atomic cleanups; one commit per CLN.

## Tasks

1. **CLN-01** — Remove `Asset.url` phantom field. Type + write site + client fallbacks.
2. **CLN-02** — Unify sprite URL naming on `spriteSignedUrl` across list + on-demand responses; update AssetCard.
3. **CLN-03** — Expand `UploadCompleteRequest` with fields the server actually reads (`width?`, `height?`, `duration?`, `frameRate?`, `thumbnailGcsPath?`).
4. **CLN-04** — Port `AbortController` pattern from `useComments` into `useAssets.fetchAssets`.
5. **CLN-05** — Rewrite `folderIsAccessible` to O(1) check via `Folder.path[]`.
6. **CLN-06** — Properly await `writer.destroy()` + `reader.cancel()` + `writer.once('close')` on size-exceeded abort path in `generate-sprite`.
7. **CLN-07** — Tighten `probed: false` semantics in `upload/complete` for video assets with client-provided dims.

## Verification

- Typecheck + vitest pass after each task.
- No grep hits for `asset.url` in client code after CLN-01.
- AssetCard reads `spriteSignedUrl` only (no `spriteStripUrl` fallback for new response).
