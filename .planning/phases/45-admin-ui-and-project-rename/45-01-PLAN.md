---
phase: 45-admin-ui-and-project-rename
plan: 01
type: execute
wave: 1
depends_on: [44-01]
files_modified:
  - src/app/api/admin/users/[userId]/revoke-sessions/route.ts
  - src/app/api/admin/users/orphans/route.ts
  - src/app/api/admin/users/[userId]/route.ts
  - src/app/api/admin/projects/[projectId]/permissions/route.ts
  - src/app/api/projects/[projectId]/route.ts
  - src/app/(app)/admin/page.tsx
  - src/components/admin/UserTable.tsx
  - src/components/admin/ProjectPermissionsPanel.tsx
  - src/components/admin/OrphanUsersPanel.tsx
  - src/components/admin/UserSessionActions.tsx
  - src/components/projects/ProjectCard.tsx
  - src/components/projects/RenameProjectModal.tsx
autonomous: false
requirements: [ACCESS-04, ACCESS-05, ACCESS-06, PROJ-01]

must_haves:
  truths:
    - "Admin can open a project-permissions view that lists collaborators (with role), review links (with flags + creator + expiresAt), and pending invites — all without opening Firestore console"
    - "Admin can click 'Suspend' on any user row and that user's future requests are rejected (existing session rejected on next refresh)"
    - "Admin can click 'Revoke sessions' on any user row and the user's Firebase refresh tokens are invalidated — their existing ID token becomes stale within the auth window"
    - "Admin can open an 'Orphan users' view listing users with no invitation history and no active collaboration, and bulk-suspend or delete them"
    - "A project owner or platform admin can rename a project via context menu → modal; a name collision within the same owner's projects blocks the save with a clear error"
  artifacts:
    - path: "src/app/api/admin/users/[userId]/revoke-sessions/route.ts"
      provides: "POST endpoint that calls admin SDK revokeRefreshTokens"
      exports: ["POST"]
    - path: "src/app/api/admin/users/orphans/route.ts"
      provides: "GET endpoint returning users not referenced by any project.collaborators, not invited, role=viewer"
      exports: ["GET"]
    - path: "src/app/api/admin/projects/[projectId]/permissions/route.ts"
      provides: "GET endpoint returning { project, collaborators, reviewLinks, pendingInvites } in one shot"
      exports: ["GET"]
    - path: "src/components/admin/ProjectPermissionsPanel.tsx"
      provides: "Modal/drawer showing the full permission state of a single project with action buttons"
    - path: "src/components/admin/OrphanUsersPanel.tsx"
      provides: "Tab panel listing orphan users with bulk suspend/delete actions"
    - path: "src/components/admin/UserSessionActions.tsx"
      provides: "Suspend + Revoke sessions buttons embedded in UserTable rows"
    - path: "src/components/projects/RenameProjectModal.tsx"
      provides: "Rename modal with collision check against owner's other projects"
  key_links:
    - from: "src/components/admin/ProjectPermissionsPanel.tsx"
      to: "/api/admin/projects/[projectId]/permissions"
      via: "fetch on mount"
      pattern: "fetch.*admin/projects.*permissions"
    - from: "src/components/admin/UserSessionActions.tsx"
      to: "/api/admin/users/[userId]/revoke-sessions"
      via: "POST on click"
      pattern: "revoke-sessions"
    - from: "src/components/admin/OrphanUsersPanel.tsx"
      to: "/api/admin/users/orphans"
      via: "fetch on tab open"
      pattern: "admin/users/orphans"
    - from: "src/components/projects/RenameProjectModal.tsx"
      to: "/api/projects/[projectId]"
      via: "PATCH/PUT with { name } after collision pre-check"
      pattern: "projects/\\$\\{.*\\.id\\}"
---

<objective>
Deliver the admin UI surface for the Phase 44 access rewrite: auditing permissions per project, suspending users + revoking sessions, cleaning up orphan users, and renaming projects (owner/admin).

Purpose: Close ACCESS-04/05/06 and PROJ-01. Phase 44 centralized authorization in `src/lib/permissions.ts`; this phase gives admins and owners the in-app surfaces to use it — no more opening Firestore console to audit permission state or disable a bad actor.

Output: New admin sub-views (ProjectPermissionsPanel, OrphanUsersPanel), new per-row user actions (Suspend + Revoke sessions), new POST /revoke-sessions + GET /orphans + GET /permissions endpoints, and a RenameProjectModal wired into ProjectCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-access-model-enforcement/44-01-SUMMARY.md
@src/app/(app)/admin/page.tsx
@src/components/admin/UserTable.tsx
@src/components/admin/SafeZonesManager.tsx
@src/app/api/admin/users/route.ts
@src/app/api/admin/users/[userId]/route.ts
@src/app/api/projects/[projectId]/route.ts
@src/components/projects/ProjectCard.tsx
@src/components/projects/CollaboratorsPanel.tsx
@src/lib/firebase-admin.ts

<interfaces>
<!-- Key contracts extracted from existing code. Use these directly; do not re-explore. -->

From src/lib/firebase-admin.ts:
```typescript
export function getAdminAuth(): Auth;   // firebase-admin/auth
export function getAdminDb(): Firestore; // firebase-admin/firestore
// getAdminAuth().revokeRefreshTokens(uid: string): Promise<void>
```

From src/lib/auth-helpers.ts (existing — do not modify):
```typescript
export async function requireAdmin(request: NextRequest): Promise<User | null>;
export async function getAuthenticatedUser(request: NextRequest): Promise<User | null>;
```

From src/lib/permissions.ts (Phase 44 — consume, do not modify):
```typescript
export function canRenameProject(user: User, project: Project): boolean;
export function getProjectRole(user: User, project: Project): ProjectRole | null;
```

From src/types/index.ts (existing):
```typescript
interface User { id: string; email: string; name: string; role: 'admin'|'manager'|'editor'|'viewer';
                  avatar?: string; invited?: boolean; disabled?: boolean; createdAt: Timestamp; }
interface Project { id: string; name: string; description?: string; color?: string;
                     ownerId: string; collaborators?: Collaborator[]; createdAt: Timestamp; updatedAt: Timestamp; }
interface Collaborator { userId: string; name: string; email: string; role: 'owner'|'editor'|'reviewer'; }
interface ReviewLink { token: string; projectId: string; folderId?: string; assetIds?: string[];
                        createdBy: string; expiresAt?: Timestamp; password?: string;
                        allowComments: boolean; allowDownloads: boolean; allowApprovals: boolean;
                        showAllVersions: boolean; }
```

From existing admin PATCH /api/admin/users/[userId]:
- Already supports `{ disabled: true|false }` and calls `revokeRefreshTokens` internally on disable.
- Task 2 (UserSessionActions) wires Suspend to this existing PATCH.
- Task 3 (revoke-sessions endpoint) is a SEPARATE action that revokes WITHOUT changing disabled — for "kick out active sessions" without suspending.

From src/app/api/projects/[projectId]/route.ts:
- `PUT` already whitelists `['name', 'description', 'color', 'coverImage']` and is gated by `canRenameProject`. It does NOT currently check name collision. Task 8 adds a PATCH variant (or extends PUT) with collision detection.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/admin/projects/[projectId]/permissions — unified permission state endpoint</name>
  <files>src/app/api/admin/projects/[projectId]/permissions/route.ts</files>
  <action>
    Create `GET /api/admin/projects/[projectId]/permissions` guarded by `requireAdmin`. Implements ACCESS-04 data layer.

    Response shape:
    ```ts
    {
      project: { id, name, ownerId, ownerName, ownerEmail },
      collaborators: Collaborator[],      // from project.collaborators, hydrated with current user.disabled flag if available
      reviewLinks: Array<{
        token, folderId?, assetIds?, createdBy, createdByName, createdAt, expiresAt?,
        allowComments, allowDownloads, allowApprovals, showAllVersions, hasPassword: boolean
      }>,
      pendingInvites: Array<{ userId, name, email }>  // users with invited=true whose email appears in project.collaborators
    }
    ```

    Implementation:
    - Load project doc; 404 if missing.
    - Hydrate owner from users collection.
    - Query `reviewLinks.where('projectId', '==', projectId)` — do NOT return raw `password`; only `hasPassword: !!link.password`.
    - For each review link `createdBy`, resolve name via users collection (batch gets).
    - Pending invites: filter `project.collaborators` whose userId matches a user doc with `invited === true`.
    - Never expose password hash. 500 on unexpected errors (log with console.error).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    curl: `GET /api/admin/projects/{id}/permissions` with admin token returns 200 + JSON matching shape; with non-admin returns 403.
  </verify>
  <done>Endpoint returns a single JSON payload covering collaborators, review links (with flags + creator + expiresAt), and pending invites. Password values are never in the response.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectPermissionsPanel component — audit view with action buttons [UI-hint]</name>
  <files>src/components/admin/ProjectPermissionsPanel.tsx, src/app/(app)/admin/page.tsx</files>
  <action>
    Implements ACCESS-04 UI surface.

    `ProjectPermissionsPanel` props: `{ projectId: string; onClose: () => void; getIdToken: () => Promise<string|null> }`.
    Render as a Modal (reuse `@/components/ui/Modal`) sized `lg`. Three sections:

    1. **Collaborators** — list each with Avatar, name, email, role Badge (reuse colors from `CollaboratorsPanel.tsx`: owner=purple, editor=success, reviewer=info). Per row: a role `<select>` wired to existing `PUT /api/projects/[projectId]/collaborators` (body `{ userId, role }`). Owner row read-only.
    2. **Review Links** — table: token (first 8 chars), creator name, created date, expiresAt (or "Never"), flag icons (comments/downloads/approvals/allVersions using lucide icons, muted if false), password lock icon if hasPassword. Per row: "Revoke" button → `DELETE /api/review-links/[token]`, with confirm inline.
    3. **Pending Invites** — simple list of name + email with "Pending" badge.

    Loading state: Spinner. Empty state: muted "No review links" / "No collaborators".

    Wire into `src/app/(app)/admin/page.tsx`:
    - Add state `const [permsProjectId, setPermsProjectId] = useState<string|null>(null)`.
    - In the existing `<ProjectsTable>` render in the "All Projects" tab, pass `onInspectPermissions={setPermsProjectId}` (or attach click on project row — do NOT rewrite ProjectsTable layout, just add the callback).
    - Render `{permsProjectId && <ProjectPermissionsPanel projectId={permsProjectId} onClose={() => setPermsProjectId(null)} getIdToken={getIdToken} />}` at the bottom of AdminPage.

    Anti-scope: Do not rewrite ProjectsTable. Do not add new role types. Do not implement email-notification hooks when revoking links.

    Use `react-hot-toast` for success/error feedback. Match existing dark theme (`bg-frame-card`, `border-frame-border`, `text-frame-accent`).

    UI-hint: Spinner for load, toast for action results, empty states, and muted/filled icon style for review-link flags.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    Manual: Open /admin → All Projects tab → click a project → panel opens → collaborators + review links + pending invites all visible in one modal.
  </verify>
  <done>Admin UI surfaces full permission state of any project in a single view without touching Firestore console. Role changes + review-link revoke work through the panel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: POST /api/admin/users/[userId]/revoke-sessions — standalone revoke endpoint</name>
  <files>src/app/api/admin/users/[userId]/revoke-sessions/route.ts</files>
  <action>
    Implements ACCESS-05 revoke-sessions path as a SEPARATE action from suspend (existing PATCH handles suspend+revoke together; this is revoke-only).

    - `POST` guarded by `requireAdmin`.
    - Reject `params.userId === admin.id` with 400 "Cannot revoke your own sessions".
    - Call `getAdminAuth().revokeRefreshTokens(params.userId)`.
    - Catch and log errors; return 200 even if the user has no Firebase Auth record (invited-only user) — respond `{ success: true, hadAuthRecord: boolean }`.
    - No Firestore writes (this action does NOT set disabled=true; it only kicks active tokens).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    curl: `POST /api/admin/users/{uid}/revoke-sessions` with admin token → 200 `{ success: true }`; self → 400; non-admin → 403.
  </verify>
  <done>Endpoint invalidates a user's refresh tokens without toggling their disabled flag. Safe for users without Firebase Auth records.</done>
</task>

<task type="auto">
  <name>Task 4: UserSessionActions — Suspend + Revoke buttons on UserTable rows [UI-hint]</name>
  <files>src/components/admin/UserSessionActions.tsx, src/components/admin/UserTable.tsx</files>
  <action>
    Implements ACCESS-05 UI surface.

    `UserSessionActions` props: `{ user: User; onSuspendToggle: (userId: string, disabled: boolean) => Promise<void>; onRevoke: (userId: string) => Promise<void>; isSelf: boolean }`.

    Render two small buttons (or overflow menu) in the UserTable Actions cell:
    - **Suspend / Reactivate** — toggles `user.disabled`. Wires to existing `PATCH /api/admin/users/[userId]` with `{ disabled: true|false }`. Label switches based on current state. Hidden for self.
    - **Revoke sessions** — calls new `POST /api/admin/users/[userId]/revoke-sessions`. Confirm inline ("Sure?" → "Yes, revoke"). Hidden for self.

    Hook into `UserTable.tsx`:
    - Accept new optional props on `UserTableProps`: `onSuspendToggle?`, `onRevoke?`.
    - In the Actions `<td>`, render `<UserSessionActions>` alongside the existing Delete button.
    - The existing "Suspended" badge (line 95-99 of UserTable.tsx) already displays the disabled state — keep it.

    Hook into `src/app/(app)/admin/page.tsx`:
    - Add `handleSuspendToggle(userId, disabled)`: PATCH /api/admin/users/[userId], update local state users[].disabled, toast success/error.
    - Add `handleRevoke(userId)`: POST .../revoke-sessions, toast "Sessions revoked".
    - Pass both to `<UserTable>`.

    UI-hint: Inline confirm pattern (reuse the "Sure? / Yes, X / Cancel" style already in UserRow delete block). Toasts for result. Loading spinner on in-flight.

    Anti-scope: Do not build a dedicated modal. Do not add email notifications. Do not add bulk-select.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    Manual: Admin panel → Users tab → Suspend a test user → the user's row shows "Suspended" badge; the user cannot refresh their session.
  </verify>
  <done>Admin can suspend/reactivate users and revoke sessions from the Users table row actions. Suspended users cannot establish new sessions.</done>
</task>

<task type="auto">
  <name>Task 5: GET /api/admin/users/orphans — orphan user query</name>
  <files>src/app/api/admin/users/orphans/route.ts</files>
  <action>
    Implements ACCESS-06 data layer.

    `GET /api/admin/users/orphans` guarded by `requireAdmin`. Query params: `?limit=50&cursor={userId}`.

    Definition (per CONTEXT Claude's Discretion):
    Orphan = user with `invited !== true` AND user.id NOT present in any `project.collaborators[].userId` AND user.id NOT equal to any `project.ownerId` AND `role === 'viewer'`.

    Implementation (simple + correct, not premature-optimized):
    - Load all projects once (`db.collection('projects').get()`). Build `Set<string>` of all userIds that are owner OR collaborator.
    - Load users `where('role', '==', 'viewer')` and `where('invited', '!=', true)` — Firestore does not support `!=` across != and == together without compound index, so: load all viewers, filter in memory for `invited !== true`.
    - Filter out users whose id is in the project-membership set.
    - Sort by createdAt desc. Apply cursor + limit (simple slice after startAt).
    - Return `{ users: User[], nextCursor: string | null }`.

    Note: This is an admin-only endpoint with a bounded user count (no tenanting). In-memory filter is acceptable for v1.7.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    curl: `GET /api/admin/users/orphans` with admin token returns 200 + `{ users: [...], nextCursor }`; users returned have `invited !== true` and are not collaborators on any project.
  </verify>
  <done>Endpoint returns paginated list of orphan users (never invited, never collaborated, role=viewer) for admin cleanup.</done>
</task>

<task type="auto">
  <name>Task 6: OrphanUsersPanel + admin tab wiring [UI-hint]</name>
  <files>src/components/admin/OrphanUsersPanel.tsx, src/app/(app)/admin/page.tsx</files>
  <action>
    Implements ACCESS-06 UI surface.

    `OrphanUsersPanel` props: `{ getIdToken: () => Promise<string|null> }`.
    - Fetches `/api/admin/users/orphans` on mount + on refresh click.
    - Renders table: checkbox, Avatar, name, email, createdAt (relative).
    - Header: "N orphan users" + "Refresh" button + bulk action bar that appears when ≥1 selected.
    - Bulk actions: "Suspend selected" (PATCH each with disabled=true), "Delete selected" (DELETE each via existing DELETE /api/admin/users). Show inline confirm before bulk delete with count.
    - Loading/empty states. Toast for bulk results: "3 users suspended" / "2 failed".

    Wire into `src/app/(app)/admin/page.tsx`:
    - Add new tab key `'orphans'` alongside existing 'users' | 'projects' | 'safezones'.
    - Tab label "Orphan Users", icon `UserX` from lucide-react.
    - Render panel in the tab content area matching existing `bg-frame-card border border-frame-border rounded-xl` wrapper.
    - On bulk-change, no refetch of main Users tab needed until user switches back (simpler).

    Anti-scope: Do NOT rewrite the existing tab bar layout — just add one more entry to the array. Do NOT add cursor pagination UI in this pass (endpoint supports it; panel can load first 50).

    UI-hint: Bulk selection via header checkbox + per-row checkbox. Selected count bubble above actions. Matches existing admin dark theme.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    Manual: /admin → Orphan Users tab → shows list of never-invited viewer users who aren't on any project → select 2 → Delete selected → toast "2 users deleted" → list refetches.
  </verify>
  <done>Admin can audit orphan users in-app and suspend or delete them in bulk without leaving the admin panel.</done>
</task>

<task type="auto">
  <name>Task 7: PATCH /api/projects/[projectId] — add name-collision check + allow name updates</name>
  <files>src/app/api/projects/[projectId]/route.ts</files>
  <action>
    Implements PROJ-01 server path.

    The existing `PUT` already whitelists `name` and is gated by `canRenameProject` (Phase 44). Add collision detection:

    - Before writing `updates`, if `updates.name` is a non-empty string AND differs from current `project.name`:
      - Query `db.collection('projects').where('ownerId', '==', project.ownerId).get()` (bounded per user — fine for v1.7; can be optimized later).
      - If any other project doc has `name === updates.name.trim()` (case-insensitive compare), return 409 `{ error: 'A project with that name already exists', code: 'NAME_COLLISION' }`.
    - Also trim `updates.name` before writing. Reject empty/whitespace-only with 400.

    Keep everything else identical. Do not touch DELETE or GET. Do not change the authorization gate.

    Note: Even for admin override (admin is renaming someone else's project), collision check runs against the `project.ownerId`'s namespace, not the admin's — matches PROJ-01 requirement wording ("within the user's projects").
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/permissions-api.test.ts</automated>
    curl: PUT `/api/projects/{id}` with body `{ name: "Existing Project Name" }` where the owner already has a project with that name → 409 NAME_COLLISION. With a unique name → 200.
  </verify>
  <done>Project rename via PUT rejects name collisions within the owner's project set; existing access-control tests still pass.</done>
</task>

<task type="auto">
  <name>Task 8: RenameProjectModal + ProjectCard context menu integration [UI-hint]</name>
  <files>src/components/projects/RenameProjectModal.tsx, src/components/projects/ProjectCard.tsx</files>
  <action>
    Implements PROJ-01 UI surface.

    `RenameProjectModal` props: `{ project: Project; onClose: () => void; onRenamed: (newName: string) => void }`.
    - Reuse `@/components/ui/Modal` title="Rename project" size="sm".
    - Single `<input>` pre-filled with `project.name`, autofocus, select-all on mount.
    - "Rename" button + "Cancel". Enter submits; Escape cancels.
    - On submit:
      1. Trim. If empty or unchanged → close without call.
      2. PUT `/api/projects/${project.id}` with `{ name }`. Header auth via `useAuth().getIdToken()`.
      3. On 409 NAME_COLLISION → show inline red error under input "A project with that name already exists"; do NOT close modal.
      4. On 403 → toast "You don't have permission to rename this project".
      5. On 200 → toast "Renamed", call `onRenamed(name)`, close.

    Wire into `src/components/projects/ProjectCard.tsx`:
    - Extend `isOwner` check to also allow admin: `const canRename = isOwner || user?.role === 'admin'`.
    - Add state `const [renaming, setRenaming] = useState(false)`.
    - Insert "Rename" item into `menuItems` before the existing "Delete" entry (gated by `canRename`):
      `{ label: 'Rename', icon: <Edit className="w-4 h-4" />, onClick: () => setRenaming(true) }`
    - Render `{renaming && <RenameProjectModal project={project} onClose={() => setRenaming(false)} onRenamed={(newName) => { setRenaming(false); onDeleted?.(); /* triggers parent refetch */ }} />}`.
    - Note: `onDeleted` callback is mis-named for this reuse but triggers the dashboard refetch — acceptable. Alternatively add an `onChanged` prop if parent already exposes one; otherwise reuse `onDeleted`.

    Anti-scope: No inline-edit-on-card-title path in this pass — modal only. No rename from review-link pages. No bulk rename. No rename animation.

    UI-hint: Input autofocus + select-all, inline collision error in red, toast for success/non-409 errors, disabled button while submitting.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    Manual: Dashboard → context menu on an owned project → Rename → enter an existing name → inline error "already exists". Enter a new name → success toast, card updates.
  </verify>
  <done>Owner or admin can rename a project via the card context menu; name collisions within the owner's projects are detected and blocked with a clear inline error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Human verification — full phase walkthrough</name>
  <what-built>
    - /api/admin/projects/[projectId]/permissions (unified audit endpoint)
    - /api/admin/users/[userId]/revoke-sessions (standalone revoke)
    - /api/admin/users/orphans (orphan query)
    - PATCH collision check on /api/projects/[projectId]
    - ProjectPermissionsPanel, OrphanUsersPanel, UserSessionActions, RenameProjectModal
    - Admin page wired with new tab + new modal + new row actions
    - ProjectCard wired with Rename menu item
  </what-built>
  <how-to-verify>
    1. **ACCESS-04:** `/admin` → All Projects → click a project → ProjectPermissionsPanel shows collaborators, review links with flags, pending invites in one view. Revoke a review link from the panel → it disappears.
    2. **ACCESS-05 (suspend):** `/admin` → Users → Suspend a test user → "Suspended" badge appears. Log in as that user in an incognito window → session rejected.
    3. **ACCESS-05 (revoke only):** `/admin` → Users → Revoke sessions on a logged-in test user → their active tab's next API call returns 401 within ~1 minute (Firebase ID token expiry window).
    4. **ACCESS-06:** `/admin` → Orphan Users tab → list renders → select 2 → Delete selected → both removed; re-open tab confirms they're gone.
    5. **PROJ-01 (success):** Dashboard → owned project context menu → Rename → new unique name → toast success → card shows new name after refetch.
    6. **PROJ-01 (collision):** Same flow but enter the name of another project you own → inline red error; save is blocked.
    7. **PROJ-01 (admin):** Log in as admin (not owner) → Rename someone else's project → collision check uses owner's namespace.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` success
- `npx vitest run` — all 116 existing tests still pass (no regression on permissions.ts consumers)
- Manual checkpoint above
</verification>

<success_criteria>
1. ACCESS-04: ProjectPermissionsPanel shows collaborators + review links (with flags + creator + expiresAt) + pending invites in one modal, no Firestore console needed.
2. ACCESS-05: Suspend toggle + Revoke sessions button both work; suspended users cannot establish new sessions; revoked users' existing tokens become stale.
3. ACCESS-06: Orphan Users tab lists orphan users and supports bulk suspend/delete.
4. PROJ-01: Owner or admin can rename a project; collision within owner's projects returns 409 and surfaces inline in the modal.
5. No regression in Phase 44 access-control test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/45-admin-ui-and-project-rename/45-01-SUMMARY.md` using the summary template. Include:
- Final endpoint shapes (request/response)
- Component prop interfaces
- Any deferred items (e.g., cursor pagination UI for orphans)
- Grep verification for `requireAdmin` usage on new endpoints
</output>
