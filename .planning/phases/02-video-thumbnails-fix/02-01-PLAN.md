---
phase: 02-video-thumbnails-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useAssets.ts
autonomous: true
requirements:
  - REQ-04

must_haves:
  truths:
    - "Thumbnail is captured at 25% of video duration (max 5s) instead of 10% (max 1s)"
  artifacts:
    - path: "src/hooks/useAssets.ts"
      provides: "captureThumbnail with better frame selection"
      contains: "video.duration * 0.25, 5"
  key_links: []
---

<objective>
Fix thumbnail frame selection so captured frames are representative rather than near-black intro frames.

Purpose: The current `captureThumbnail()` seeks to `Math.min(duration * 0.1, 1)` which almost always lands on the first second (black frame). Changing to `Math.min(duration * 0.25, 5)` picks a frame at 25% of the video (capped at 5s), which is far more representative.
Output: Updated `captureThumbnail()` in `src/hooks/useAssets.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useAssets.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix captureThumbnail seek time</name>
  <files>src/hooks/useAssets.ts</files>
  <read_first>src/hooks/useAssets.ts (lines 44-51 — the loadedmetadata listener)</read_first>
  <action>
In `src/hooks/useAssets.ts`, inside the `captureThumbnail` function, find the `loadedmetadata` event listener (line 46-49). Change line 48 from:

```
video.currentTime = Math.min(video.duration * 0.1, 1) || 1;
```

to:

```
video.currentTime = Math.min(video.duration * 0.25, 5) || 1;
```

Also update the comment on line 47 from:
```
// Seek to 10% of duration or 1s, whichever is smaller — avoids black intro frames
```
to:
```
// Seek to 25% of duration or 5s, whichever is smaller — avoids black intro frames
```

This addresses REQ-04. No other changes needed.
  </action>
  <verify>
    <automated>grep -n "duration \* 0.25, 5" src/hooks/useAssets.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "video.duration \* 0.25, 5" src/hooks/useAssets.ts` returns exactly one match on the currentTime assignment line
    - `grep "video.duration \* 0.1" src/hooks/useAssets.ts` returns zero matches (old value is gone)
    - `grep "25% of duration" src/hooks/useAssets.ts` returns one match (updated comment)
  </acceptance_criteria>
  <done>captureThumbnail seeks to 25% of duration (max 5s) instead of 10% (max 1s)</done>
</task>

</tasks>

<verification>
- `grep "0.25, 5" src/hooks/useAssets.ts` returns the updated line
- `grep "0.1, 1" src/hooks/useAssets.ts` returns nothing (old value removed)
- `npx tsc --noEmit` passes (no type errors introduced)
</verification>

<success_criteria>
The captureThumbnail function seeks to Math.min(video.duration * 0.25, 5) for more representative frames.
</success_criteria>

<output>
After completion, create `.planning/phases/02-video-thumbnails-fix/02-01-SUMMARY.md`
</output>
